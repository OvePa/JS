/*
forEach method
The forEach method of an array works much like a loop. The forEach method applies a function for each element of the array.
*/

var arr = [1, 2, 3, 4, 5, 6, 7, 8];
// iterate array from start to end
arr.forEach((x) => console.log(x));
